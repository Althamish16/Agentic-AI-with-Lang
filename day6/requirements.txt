# ─────────────────────────────────────────────────────────────────────────────
# Day 6 · Multi-agent + resumable workflows — minimal, offline-first deps.
#
# The demos ALL run with just LangChain + LangGraph and the built-in mock
# model (day6/_llm.py). langchain-openai / langchain-anthropic are optional:
# install one of them (plus its API key) if you want to swap the mock for a
# real provider via DAY6_PROVIDER=openai / anthropic.
# ─────────────────────────────────────────────────────────────────────────────

# Core (required)
langgraph>=0.2.60,<0.7                 # StateGraph, conditional edges, streams
langchain-core>=0.3.40,<0.4            # BaseMessage, tool decorator
langgraph-checkpoint-sqlite>=2.0,<3    # SqliteSaver — durable resume

# Optional providers (uncomment the one you want, or set DAY6_PROVIDER=mock)
# langchain-openai>=0.2.14,<0.4        # OPENAI_API_KEY
# langchain-anthropic>=0.2.0,<0.4      # ANTHROPIC_API_KEY

# Stretch goal in the exercise
# langgraph-supervisor>=0.0.10,<0.2
