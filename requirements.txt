# ─────────────────────────────────────────────────────────────────────────────
# Research Assistant Labs — dependencies (lightly pinned)
# One virtual env powers all 7 days + the web UI.
# ─────────────────────────────────────────────────────────────────────────────

# --- Core LangChain / LangGraph ---
langchain>=0.3.20,<0.4
langchain-core>=0.3.40,<0.4
langchain-community>=0.3.20,<0.4
langchain-openai>=0.2.14,<0.4        # Azure OpenAI chat + embeddings
langchain-chroma>=0.1.4,<0.3         # thin Chroma vector-store wrapper
langgraph>=0.2.60,<0.7               # StateGraph, ToolNode, interrupt(), Command
langgraph-checkpoint-sqlite>=2.0,<3  # durable checkpointer for Day 5/6 resume

# --- Vector store + embeddings ---
chromadb>=0.5.5,<0.7                  # local, file-based vector DB (no server)
fastembed>=0.3.6,<0.8                 # lightweight local embeddings (ONNX, no torch)

# --- Observability (Day 7) ---
langsmith>=0.1.140,<0.5

# --- Config / models ---
python-dotenv>=1.0.1,<2
pydantic>=2.7,<3
tiktoken>=0.7,<1                      # token counting / splitters

# --- Web UI backend (FastAPI :5000) ---
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
sse-starlette>=2.0,<3                 # Server-Sent Events streaming of agent trace

# --- Day 2 live demo app (streamlit run day2/demos/app.py) ---
streamlit>=1.40,<2                    # interactive RAG pipeline demo UI
pypdf>=4.2,<7                         # PDF uploads in the Day 2 demo
